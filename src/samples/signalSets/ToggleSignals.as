package sample.signalSets
{
    import nanosome.flow.signals.AbstractSignalSet;
    import nanosome.flow.signals.Signal;

    public class ToggleSignals extends AbstractSignalSet
    {
		// signals
		public var turnOn:Signal;
		public var turnOff:Signal;
    }
}
